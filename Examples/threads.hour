func myThread()
    local i = 1;

    while i <= 10 do
        println(i);
        sleep(1);
        i += 1;
    end;
end;

thread1 = Thread(myThread);
thread2 = Thread(myThread);

thread1.start();
thread2.start();

thread2.join();